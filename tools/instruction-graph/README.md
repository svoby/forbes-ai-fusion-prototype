# Instruction Graph Generator

Short usage note only: this tool generates diagnostic artifacts, not policy.

Run from the repository root:

```powershell
powershell -ExecutionPolicy Bypass -File .\tools\instruction-graph\generate-instruction-graph.ps1
```

Outputs are written to `tools/instruction-graph/out/` and include the generated-artifact banner.
